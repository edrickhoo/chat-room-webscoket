{
  "name": "chat-room",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "server": "nodemon index.js",
    "logs": "heroku logs --tail",
    "deploy": "git push heroku HEAD:master",
    "deploy:full": "git add . && git commit -m 'new deployment' && git push && npm run deploy"
  },
  "description": "my first socket.io app",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "socket.io": "^4.6.2"
  }
}
